function bold() {
	 const c = document.querySelector('#npa');
	 c.style.fontWeight = c.style.fontWeight === "bold" ? "400" : "bold";
}
function uppercase() {
	 const c = document.querySelector('#npa');
	 c.style.textTransform = c.style.textTransform === "uppercase" ? "capitalize" : "uppercase";
}

function boldB() {
  const c = document.querySelector('#statuS');
  c.style.fontWeight = c.style.fontWeight === "bold" ? "400" : "bold";
}

function uppercaseB() {
  const c = document.querySelector('#statuS');
  c.style.textTransform = c.style.textTransform === "uppercase" ? "capitalize" : "uppercase";
}

function boldD() {
  const c = document.querySelector('#bName');
  c.style.fontWeight = c.style.fontWeight === "bold" ? "400" : "bold";
}

function uppercaseD() {
  const c = document.querySelector('#bName');
  c.style.textTransform = c.style.textTransform === "uppercase" ? "capitalize" : "uppercase";
}
function boldE() {
  const c = document.querySelector('#clsp');
  c.style.fontWeight = c.style.fontWeight === "bold" ? "400" : "bold";
}

function uppercaseE() {
  const c = document.querySelector('#clsp');
  c.style.textTransform = c.style.textTransform === "uppercase" ? "capitalize" : "uppercase";
}

function boldF() {
  const c = document.querySelector('#daTe');
  c.style.fontWeight = c.style.fontWeight === "bold" ? "400" : "bold";
}

function boldI() {
  const c = document.querySelector('#wishess');
  c.style.fontWeight = c.style.fontWeight === "bold" ? "400" : "bold";
}

function uppercaseI() {
  const c = document.querySelector('#wishess');
  c.style.textTransform = c.style.textTransform === "uppercase" ? "capitalize" : "uppercase";
}



function nonBorder(){
	let c = document.querySelector('#logo');
 c.style.border =  "none";
}
function nonBorderA() {
  let c = document.querySelector('#pp');
  c.style.border = "none";
}